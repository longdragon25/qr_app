import 'package:flutter/material.dart';

class HeightBox extends SizedBox {
  const HeightBox(double height) : super(height: height);

  const HeightBox.one() : super(height: 1);
}

class WidthBox extends SizedBox {
  const WidthBox(double width) : super(width: width);
}
